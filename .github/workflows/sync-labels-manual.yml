# GENERATED: scripts/sync-workflow-callers.sh により生成
# ラベル同期（手動実行 / Reusable Workflow呼び出し）
#
# 使用方法:
#   1. このファイルを対象リポジトリの .github/workflows/sync-labels-manual.yml にコピー
#   2. Actions > Sync Labels > Run workflow で手動実行
#
# 呼び出し先:
#   - hasegawa496/.github/.github/workflows/sync-labels.yml（Reusable Workflow）
#   - hasegawa496/.github/.github/labels.yml（ラベル定義）

name: Sync Labels (Manual)

on:
  workflow_dispatch:  # 手動実行

permissions:
  contents: read
  issues: write

jobs:
  sync:
    uses: ./.github/workflows/sync-labels.yml
    secrets: inherit
