# scripts/

手元実行用の補助スクリプトです（`bash` 前提）。

## 一覧

- `setup.sh`: 標準構成のまとめ実行（設定 → ラベル同期）
- `setup-repo-settings.sh`: リポジトリ設定の初期化（ブランチ自動削除など）
- `setup-label-sync.sh`: ラベル同期 workflow を導入し、可能ならそのまま起動
- `setup-triage-project-fields.sh`: Issue トリアージ workflow を導入（Issue フォーム値 → Project フィールド反映）
